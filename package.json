{
    "name": "@darthcav/ts-utils",
    "description": "A collection of utility functions for TypeScript applications and modules",
    "version": "0.10.10",
    "author": {
        "name": "darthcav",
        "url": "https://github.com/darthcav"
    },
    "license": "Apache-2.0",
    "type": "module",
    "publishConfig": {
        "access": "public"
    },
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        }
    },
    "files": [
        "src",
        "!src/__tests__",
        "dist",
        "!dist/__tests__"
    ],
    "dependencies": {
        "@logtape/logtape": "2.3.0"
    },
    "devDependencies": {
        "@biomejs/biome": "2.5.7",
        "@types/node": "26.2.0",
        "asserttt": "1.0.1",
        "prettier": "3.9.6",
        "shx": "0.4.0",
        "typedoc": "0.28.20"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/darthcav/ts-utils.git"
    },
    "engines": {
        "node": ">=26"
    },
    "scripts": {
        "\n===== Build =====": "",
        "clean": "shx rm -rf dist/*",
        "build": "npm run clean && tsc",
        "prepare": "npm run build",
        "\n===== Typecheck =====": "",
        "typecheck": "tsc --noEmit",
        "\n===== Test =====": "",
        "test": "node --env-file=.env.local --experimental-test-module-mocks --test --test-reporter=spec 'src/**/*.test.ts'",
        "test:coverage": "node --env-file=.env.local --experimental-test-module-mocks --experimental-test-coverage --test 'src/**/*.test.ts'",
        "test:coverage:lcov": "shx mkdir -p coverage && node --env-file=.env.local --experimental-test-module-mocks --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-reporter=spec --test-reporter-destination=stdout --test 'src/**/*.test.ts'",
        "\n===== Linter =====": "",
        "lint": "biome check . && prettier --check \"**/*.md\" --ignore-path .gitignore",
        "lint:fix": "biome check --write . && prettier --write \"**/*.md\" --ignore-path .gitignore",
        "\n===== TypeDoc =====": "",
        "doc": "shx rm -rf public/* && typedoc --tsconfig tsconfig.json"
    }
}
